# Server
PORT=8000
NODE_ENV=development

# Database — Neon PostgreSQL
DATABASE_URL="postgresql_url"

# Auth
JWT_SECRET="replace-with-openssl-rand-hex-32-output"
JWT_EXPIRES_IN="7d"

# CORS — comma-separated for multiple origins
CORS_ORIGIN="https://frontend.com,http://localhost:3000"

# Gemini AI
GEMINI_API_KEY="AIza..."
GEMINI_FREE_LIMIT=10

# Encryption — for storing user Gemini API keys at rest (AES-256-GCM)
ENCRYPT_SECRET="replace-with-openssl-rand-hex-32-output"

# Cloudinary
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"
